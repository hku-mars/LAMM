#ifndef KMALGORITHM_H
#define KMALGORITHM_H


#include <vector>
#include <ros/ros.h>
#include <Eigen/Dense>



class KMAlgorithm{
public:
//   int maxCompatibilitySum(std::vector<std::vector<int>>& students, std::vector<std::vector<int>>& mentors,\
//                           std::vector<int> &match);

//   int KM(int n, std::vector<std::vector<int>> &c, std::vector<int> &match);

//   // 匈牙利算法寻找完美匹配
//   bool dfs(int i, int n, std::vector<std::vector<int>>& c, std::vector<int>& match, std::vector<bool>& visStudent, \
        //    std::vector<bool>& visTeacher, std::vector<int>& exStudent, std::vector<int>& exTeacher);
};

#endif
